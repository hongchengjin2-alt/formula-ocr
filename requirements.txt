pix2tex[gui]
PySide6>=6.5
python-docx
latex2mathml
lxml
matplotlib
Pillow

# Optional UniMERNet engine. This conflicts with pix2tex's timm==0.5.4 pin,
# so install it in a separate environment when both engines must be stable.
# unimernet[full]
